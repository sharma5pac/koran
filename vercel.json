{
    "cleanUrls": true,
    "rewrites": [
        {
            "source": "/((?!_expo|assets|favicon|sw.js|manifest|robots.txt).*)",
            "destination": "/index.html"
        }
    ],
    "buildCommand": "npx expo export --platform web",
    "outputDirectory": "dist"
}