{
    "rewrites": [
        {
            "source": "/((?!_expo|assets|favicon.ico|sw.js|manifest.json|manifest.webmanifest).*)",
            "destination": "/index.html"
        }
    ],
    "buildCommand": "npx expo export --platform web",
    "outputDirectory": "dist"
}